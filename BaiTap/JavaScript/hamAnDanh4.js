const numbers = [1, 2, 3, 4];

// numbers.forEach(function(num) {
//     console.log(num + 1);
// });

// Viết gọn hơn bằng Arrow Function

// numbers.forEach((num) => { console.log(num + 1);});

// Hoặc
numbers.forEach(num => console.log(num + 1));
